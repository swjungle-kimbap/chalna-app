import Realm from "realm";
import {RealmProvider} from '@realm/react';
import {ChatRoom, ChatRoomMember, ChatMessage} from "./models/ChatRoomSchema";

export const AppWrapper = () => {
    return (
        <DynamicRealmProvider
    )
}
